## Fix Obsidian marketplace review feedback

tool: codex

- Released 1.0.7 to address marketplace automated review feedback from 1.0.6.
- Removed the redundant word "Obsidian" from the plugin manifest description.
- Replaced `FileManager.trashFile` with `Vault.trash(file, true)` to keep `minAppVersion` at `1.6.0` while preserving trash-based cleanup for regenerated managed images.
- Verified with `npm run typecheck`, `npm run test`, and `npm run obsidian:dev`.
